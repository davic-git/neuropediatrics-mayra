/**
 * O site estático original (index.html / style.css / script.js) não possuía
 * uma seção de depoimentos, então não há conteúdo de origem para migrar aqui.
 * Este componente ficou como stub para quando houver depoimentos reais —
 * ele não é importado em App.tsx enquanto isso.
 */
export default function Testimonials() {
  return null;
}
